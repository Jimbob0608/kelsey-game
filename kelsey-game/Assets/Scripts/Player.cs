
using UnityEngine;

public class Player : MonoBehaviour {
    [ShowOnly] public int coins;
    [ShowOnly] public int kills;
    [ShowOnly] public int dashCharges = 3;

    public int health;

    // Start is called before the first frame update
    void Start() { }

    // Update is called once per frame
    void Update() {
        
    }
}